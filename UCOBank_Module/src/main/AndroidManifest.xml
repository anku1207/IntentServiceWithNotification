<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ucobank_module">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application>

    <activity android:name="in.cbslgroup.ucobank.ui.activity.SplashScreenActivity"
        android:theme="@style/Theme.UCOBank">



    </activity>
    <!--
         The API key for Google Maps-based APIs is defined as a string resource.
         (See the file "res/values/google_maps_api.xml").
         Note that the API key is linked to the encryption key used to sign the APK.
         You need a different API key for each encryption key, including the release key that is used to
         sign the APK for publishing.
         You can define the keys for the debug and release targets in src/debug/ and src/release/.
    -->

    <activity android:name="in.cbslgroup.ucobank.ui.activity.BankMapsActivity" /> <!-- These are the main activities as used in nav graphs -->
    <!-- Single Activity Multiple Fragment Architecture -->
    <activity
        android:name="in.cbslgroup.ucobank.ui.activity.MainActivity"
        android:label="@string/app_name"
        android:theme="@style/Theme.UCOBank.NoActionBar">


    </activity>
    <activity
        android:name="in.cbslgroup.ucobank.ui.activity.LoginActivity">
    </activity>
    </application>

</manifest>